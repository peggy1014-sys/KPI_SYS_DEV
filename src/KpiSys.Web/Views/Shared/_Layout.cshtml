@{ 
    var path = Context.Request.Path.Value?.Trim('/') ?? "";
    var breadcrumb = string.IsNullOrEmpty(path) ? "🏠 /" : $"🏠 / {path}";
    var currentPath = string.IsNullOrEmpty(path) ? "/" : $"/{path.ToLower()}";
    var isAuthenticated = Context.Session.GetInt32(SessionKeys.UserId).HasValue;
    var userName = Context.Session.GetString(SessionKeys.UserName);
    var userRole = Context.Session.GetString(SessionKeys.UserRole);
}
<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - 專案工時KPI績效管理系統</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    <div class="layout d-flex">
        <aside class="sidebar d-flex flex-column">
            <div class="brand py-3 px-3">
                <h6 class="brand-title mb-0 text-white">專案工時KPI績效管理系統</h6>
            </div>
            <nav class="flex-grow-1 sidebar-nav">
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center @(currentPath == "/" ? "active" : "")" href="/">
                            <span class="me-2">📊</span> 儀表板
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center @(currentPath == "/organizations" ? "active" : "")" href="/organizations">
                            <span class="me-2">🏢</span> 組織管理
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center @(currentPath == "/organizationbrowser" ? "active" : "")" href="/organizationbrowser">
                            <span class="me-2">🌳</span> 組織瀏覽
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center @(currentPath == "/employees" ? "active" : "")" href="/employees">
                            <span class="me-2">👥</span> 員工管理
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center @(currentPath == "/codes" ? "active" : "")" href="/codes">
                            <span class="me-2">🧩</span> 代碼管理
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center @(currentPath == "/users" ? "active" : "")" href="/users">
                            <span class="me-2">🙍‍♂️</span> 使用者管理
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center @(currentPath == "/projects" ? "active" : "")" href="/projects">
                            <span class="me-2">📁</span> 專案管理
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center @(currentPath == "/timesheets" ? "active" : "")" href="/timesheets">
                            <span class="me-2">🕒</span> 工時填報
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center @(currentPath == "/timesheetapprovals" ? "active" : "")" href="/timesheetapprovals">
                            <span class="me-2">✅</span> 工時審核
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link d-flex align-items-center @(currentPath.StartsWith("/kpiadmin") ? "active" : "")" href="/kpiadmin">
                            <span class="me-2">📈</span> KPI 管理
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>
        <div class="main-content flex-grow-1 d-flex flex-column">
            <header class="topbar d-flex align-items-center px-4 py-3 border-bottom">
                <div class="text-muted small">@breadcrumb</div>
                <div class="ms-auto d-flex align-items-center gap-3">
                    @if (isAuthenticated)
                    {
                        <div class="text-end">
                            <div class="fw-semibold">@userName</div>
                            <div class="text-muted small">@userRole</div>
                        </div>
                        <form asp-controller="Auth" asp-action="Logout" method="post" class="mb-0">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-sm btn-outline-secondary">登出</button>
                        </form>
                    }
                    else
                    {
                        <a class="btn btn-sm btn-primary" href="/Auth/Login">登入</a>
                    }
                </div>
            </header>
            <main class="content-body px-4 py-4">
                @RenderBody()
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
